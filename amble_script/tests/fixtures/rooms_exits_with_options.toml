# room plaza-gate (source line 1)
[[rooms]]
id = "plaza-gate"
name = "Plaza Gate"
base_description = "A broad gateway leading into the central plaza."
location = "Nowhere"

[rooms.exits.north]
to = "security-post"
locked = true
barred_message = "Access badge required."
required_items = ["keycard", "access-form"]
required_flags = [{ type = "simple", name = "clearance-granted" }, { type = "simple", name = "maintenance-inspection" }]

[rooms.exits.south]
to = "shuttle-platform"

[rooms.exits.east]
to = "gallery-corridor"
required_items = ["keycard"]
