# Demonstrates multiple if-blocks and unconditional actions in one trigger

trigger "Multi-IF Demo" note "show how lowering works" when enter room lab {
  if has flag quest-started {
    do show "Welcome back to the lab."
  }
  if chance 25% {
    do show "A fluorescent bulb flickers overhead."
  }
  # Unconditional action (fires with the same event)
  do spinner message ambientInterior
}

